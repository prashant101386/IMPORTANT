# Pipeline that deploys the Databricks infrastructure 

trigger:
- none

variables:
- template: ../configurations/configuration.variables.yml
- template: ../configurations/prjmme/configuration-infra-DEV.variables.yml # DEV as default env

pool:
  vmImage: $(VM_VERSION)

stages:

####################
##  BUILD INFRA  ##
####################

# Lint the Bicep file.
- stage: buildinfra
  displayName: 'Build Environment'
  jobs: 
  - template: templates/run-code-lint.template.yml
    parameters:
      module: 'prj'

####################
##  DEPLOY INFRA  ##
####################

# Deploy DEV Environment
- stage: deployinfra_dev
  displayName: 'Deploy DEV Environment'
  dependsOn: buildinfra
  variables:
  - template: ../configurations/prjmme/configuration-infra-DEV.variables.yml
  jobs:
  - template: templates/deploy-env-infra.template.yml
    parameters:
      environment: MME-IAC-DEV
      serviceConnection: $(SERVICECONNECTION_RG)
      resourceGroup: $(RESOURCE_GROUP)
      location: $(LOCATION)
      subscription: $(SUBSCRIPTIONID)
      module: 'prj'
      project: 'MME'


# Deploy QA Environment
- stage: deployinfra_QA
  displayName: 'Deploy QA Environment'
  variables:
  - template: ../configurations/prjmme/configuration-infra-QA.variables.yml
  jobs:
  - template: templates/deploy-env-infra.template.yml
    parameters:
      environment: MME-IAC-QA
      serviceConnection: $(SERVICECONNECTION_RG)
      resourceGroup: $(RESOURCE_GROUP)
      location: $(LOCATION)
      subscription: $(SUBSCRIPTIONID)
      module: 'prj'
      project: 'MME'

# Deploy PROD Environment
- stage: deployinfra_PROD
  displayName: 'Deploy PROD Environment'
  variables:
  - template: ../configurations/prjmme/configuration-infra-PROD.variables.yml
  jobs:
  - template: templates/deploy-env-infra.template.yml
    parameters:
      environment: MME-IAC-PROD
      serviceConnection: $(SERVICECONNECTION_RG)
      resourceGroup: $(RESOURCE_GROUP)
      location: $(LOCATION)
      subscription: $(SUBSCRIPTIONID)
      module: 'prj' 
      project: 'MME'